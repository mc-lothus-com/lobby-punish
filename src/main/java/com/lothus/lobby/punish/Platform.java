package com.lothus.lobby.punish;

import com.lothus.lobby.punish.manager.PlayerManager;
import lombok.Getter;

public class Platform {

    @Getter
    private static PlayerManager playerManager = new PlayerManager();
}
